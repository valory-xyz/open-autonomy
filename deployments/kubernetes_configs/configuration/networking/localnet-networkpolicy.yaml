apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: localnet
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/localnet: "true"
  podSelector:
    matchLabels:
      io.kompose.network/localnet: "true"
